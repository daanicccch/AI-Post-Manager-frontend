# ChannelBot Mini App Web

Отдельный frontend-проект для редакторского Mini App интерфейса.

## Run

```bash
npm install
cp .env.example .env
npm run dev
```

По умолчанию frontend ожидает backend API на `http://localhost:3010/api`.
